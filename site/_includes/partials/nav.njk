<nav id="nav" class="nav pt-4 pb-2 md:flex md:items-stretch md:py-0">

	<div class="link-bar">
		{% for item in navigation.main %}
		<a href="{{ item.url | safe }}" class="link-bar__link font-sans-demi flex items-center text-gray-700 py-2 {% if page.url == item.url %} active{% else %} text-gray-700{% endif %}">{{ item.label }}</a>
		{% endfor %}
	</div>

</nav>
